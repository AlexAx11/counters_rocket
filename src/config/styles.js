import imgUrl from "../img/background.svg";

let styles = {
    root: {
        background: 'center',
        backgroundImage: 'url(' + imgUrl + ')',
        backgroundSize: 'cover',
        overflow: 'hidden',
        height: '100vh'
    }
}

export default styles;